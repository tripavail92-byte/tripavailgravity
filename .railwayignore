/node_modules/
/.turbo/
/packages/web/dist/
/packages/shared/dist/
/packages/python-worker/
/services/
/.git/
*.log
.env*
!.env.example
/supabase/.branches/
/supabase/migrations/*.sql
/online project/
/extracted_tripavail/
/tools/
/docs/
__pycache__
*.pyc
/.venv/
*.apk
# …except the internal test build served at /download (see DownloadPage.tsx).
!/packages/web/public/download/*.apk
# NOTE: packages/mobile IS uploaded so its package.json is present for the frozen pnpm
# install; the build only compiles @tripavail/web and the install is scoped via --filter,
# so mobile RN/Expo deps are never fetched on the web build.
