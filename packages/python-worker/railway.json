{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["packages/python-worker/**"]
  },
  "deploy": {
    "startCommand": "python -u worker.py",
    "restartPolicyType": "ALWAYS"
  }
}
