// Centralized animation durations for Hotel Manager UI

class HMAnimation {
  static const Duration quick = Duration(milliseconds: 1600);
  static const Duration normal = Duration(milliseconds: 2400);
  static const Duration calm = Duration(milliseconds: 4000);
}
